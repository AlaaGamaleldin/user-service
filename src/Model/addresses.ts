export interface Addresses{
    addresses: 
        {
          addressId: string,
          title: string,
          country: string,
          provenance: string,
          city: string,
          street: string,
          zipCode: number,
          buildingNumber: string,
          apartmentNumber: string,
          visibility: string
        }
    
}